services:
  redis:
    image: redis:latest
    container_name: redis-QueueForge
    ports:
      - "6379:6379"
    command:
      [
        "redis-server",
        "--appendonly",
        "yes"
      ]
    volumes:
      - redis-data:/data


volumes:
  redis-data:
